# Tasks: make-wizard-resizable

## 阶段 1：添加 CSS resize 支持

- [x] **T1.1** 为导入向导添加 resize 支持
  - 添加 CSS `resize: both` 和 `overflow: auto`
  - 设置最小尺寸 min-w-[800px] min-h-[500px]

- [x] **T1.2** 为导出向导添加 resize 支持
  - 添加 CSS `resize: both` 和 `overflow: auto`
  - 设置最小尺寸 min-w-[600px] min-h-[400px]

## 阶段 2：验证

- [x] **T2.1** 验证导入向导可缩放
- [x] **T2.2** 验证导出向导可缩放
- [x] **T2.3** 验证最小尺寸限制

## 完成条件

- [x] 导入向导窗口可缩放
- [x] 导出向导窗口可缩放
- [x] 最小尺寸限制生效
