# Test Environment Configuration
NODE_ENV=test

# Server
PORT=3001
API_PREFIX=/api

# Test Database (Use separate test database)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=a_teamui_test

# Test Redis (Use separate test database)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=15  # Use database 15 for tests

# JWT (Test secret - NOT for production)
JWT_SECRET=test-secret-for-testing-only
JWT_ACCESS_EXPIRY=1h
JWT_REFRESH_EXPIRY=1d

# CORS
CORS_ORIGIN=http://localhost:5173

# File Upload
UPLOAD_DIR=./test-uploads
